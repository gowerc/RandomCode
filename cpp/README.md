
The standalone scripts in this folder have been setup to run via
vscode tasks (e.g. `./Union.cpp`).  Simply open vscode so that this
folder is the root workspace and then run the "Run File" vscode task.

